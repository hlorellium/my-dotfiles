-- setup null_ls for formatting with prettier

--local null_ls = require("null-ls")
--
--null_ls.setup({
--    sources = {
--        null_ls.builtins.formatting.prettier,
--        null_ls.builtins.completion.spell,
--    },
--})
--
---- add keybindings for formatting for lua
--vim.keymap.set("n", "<leader>ll", "<cmd>lua vim.lsp.buf.format({ timeout_ms = 3000 })<CR>")


